name: derasika2
description: IIDX score csv manager.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  auto_route: ^3.0.4
  cupertino_icons: ^1.0.2
  file_picker: ^4.2.0
  flutter_hooks: ^0.18.0
  freezed_annotation: ^0.15.0
  hooks_riverpod: ^1.0.0
  json_serializable: ^6.0.1
  path_provider: ^2.0.6
  share_plus: ^3.0.4
  shared_preferences: ^2.0.8
  sqflite: ^2.0.0+4
  url_launcher: ^6.0.12
  json_annotation: ^4.3.0
  intl: ^0.17.0
  webview_flutter: ^2.3.1
  fl_chart: ^0.40.2
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^3.0.1
  freezed:
  build_runner:      

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
